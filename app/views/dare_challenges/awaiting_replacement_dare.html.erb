<div class="dare_challenge_form">
	<%= avatar_link_for @other_party %>
	
	<h1>Waiting for <%=h @other_party.name %></h1>

	<div class="form_description">
		<p>
			You rejected a dare, and you are waiting for a replacement from <%= @other_party.name %>.
		</p>
		<p>
			<%= link_to "&lt; Back to #{h(@other_party.name)}'s Profile", @other_party.profile_url %>
		</p>
	</div>
</div>